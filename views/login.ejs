<!DOCTYPE html>
<html>
    <head>
        <title>Login | Task Manager</title>
        <style>
            *{
                padding: 0;
                margin: 0;
            }
            body{
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
                position: relative;
                padding-bottom: 28px;
                min-height: 95vh;
                font-weight: 595;
            }
            header{
                min-height: 5vh;
            }
            main #flashMsg{
                margin-left: 387px;
                margin-right: 387px;
                margin-bottom: 5px;
                text-align: center;
                background-color: red;
                padding-top: 2px;
                padding-bottom: 2px;
            }
            main #fieldset{
                border: 2px solid groove;
                padding: 25px 25px;
                margin-left: 387px;
                margin-right: 387px;
            }
            main #loginBtn{
                background-color: blue;
                color: white;
                border: none;
                padding: 5px;
                border-radius: 5px;
                display: inline-block;
            }
            main #signUpLink{
                text-decoration: none;
            }
            footer{
                background-color: black;
                color: whitesmoke;
                text-align: center; 
                position: absolute;
                bottom: 0;
                width: 100%;
            }
        </style>
    </head>
    <body>
        <header></header>
        <main>
            <% if(message.length > 0){ %>
                <p id="flashMsg"><%= message %></p>
            <% } %>
            <form action="/login" method="POST">
                <fieldset id="fieldset">   
                    <center> 
                        <h1>Task Manager</h1>
                        <table>
                            <tr>
                                <td><label for="ue">username/email:</label></td>
                                <td><input type="text" name="ue" id="ue" placeholder="username or email" required></td>
                            </tr>
                            <tr>
                                <td><label for="password">password:</label></td>
                                <td><input type="password" name="password" id="password" placeholder="password" required></td>
                            </tr>
                        </table>
                        <button type="submit" name="login" id="loginBtn">Log in</button>
                        <p>Don't have an account? <a href="/signup" id="signUpLink">Sign up</a></p>
                    </center>
                </fieldset>
            </form>
        </main>
        <footer>
            <p>copyright &copy; <%= new Date().getFullYear() %> | Developer - Mohit Talreja | Email - talrejamohit15@gmail.com</p>
        </footer>
    </body>
</html>

