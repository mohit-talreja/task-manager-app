<!DOCTYPE html>
<html>
    <head>
        <title>Task Manager</title>
        <style>
            *{
                padding: 0;
                margin: 0;
            }
            body{
                background-color: whitesmoke;
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
                position: relative;
                padding-bottom: 28px;
                min-height: 95vh;
                font-weight: 595;
            }
            header{
                background-color: grey;
                text-align: center;
                min-height: 5vh;
            }
            header ul li{
                display: inline-block;
                list-style: none;
            }
            header ul li a{
                color: whitesmoke;
                text-decoration: none;
                float: left;
                padding: 5px;
            }
            header ul li a:hover{
                background-color: lime;
            }
            main{
                text-align: center;
            }
            main #addTaskBtn{
                margin-top: 5px;
                background-color: blue;
                border: none;
                border-radius: 5px;
                padding: 5px;
                display: inline-block;
            }
            main #addTaskLink{
                text-decoration: none;
                color: white;
            }
            main #profilePic{
                clip-path: circle(45%);
            }
            footer{
                background-color: black;
                color: whitesmoke;
                text-align: center; 
                position: absolute;
                bottom: 0;
                width: 100%;
            }
        </style>
    </head>
    <body>
        <header>
            <ul>
                <li><a href="/task-manager/<%= user._id %>">Home</a></li>
                <li><a href="/task-manager/<%= user._id %>/about-us">About Us</a></li>
                <li><a href="/task-manager/<%= user._id %>/contact-us">Contact Us</a></li>
                <li><a href="/task-manager/<%= user._id %>/logout">Log out</a></li>  
            </ul>
        </header>
        <main>
            <h1>Task Manager App</h1>
            <h2>Welcome, <%= user.username %></h2>
            <img id="profilePic" src="../uploads/<%= user.profilepic %>" width="250" height="250" alt="Image Not Available.">
            <p>You can add your task for today by clicking on the below button.</p>
            <button type="button" id="addTaskBtn"><a href="/task-manager/<%= user._id %>/add-task" id="addTaskLink">Go To Add Task</a></button>
        </main>
        <footer>
            <p>copyright &copy; <%= new Date().getFullYear() %> | Developer - Mohit Talreja | Email - talrejamohit15@gmail.com</p>
        </footer>
    </body>
</html>