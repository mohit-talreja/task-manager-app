<!DOCTYPE html>
<html>
    <head>
        <title>Tasks List | Task Manager</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
        <script src="https://kit.fontawesome.com/fe68cb5b10.js" crossorigin="anonymous"></script>
        <style>
            *{
                padding: 0;
                margin: 0;
            }
            body{
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
                position: relative;
                padding-bottom: 25px;
                min-height: 100vh;
                font-weight: 595;
            }
            header{
                min-height: 5vh;
            }
            main{
                text-align: center;
            }
            main #taskManagerLink{
                text-decoration: none;
            }
            footer{
                background-color: black;
                color: whitesmoke;
                text-align: center; 
                position: absolute;
                bottom: 0;
                width: 100%;
            }
        </style>
    </head>
    <body>
        <header></header>
        <main>
            <h1>Your Tasks [ <%= user.username %> ]</h1>
            <div class="container">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover">
                        <thead>
                            <tr class="table-active">
                                <th>Title</th>
                                <th>Description</th>
                                <th>Date</th>
                                <th>Time</th>
                                <th>Delete</th>
                                <th>Update</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if(tasks.length > 0){ %>
                            <% tasks.forEach(task => { %>
                            <tr>
                                <td><%= task.title %></td>
                                <td><%= task.description %></td>
                                <td><%= task.date.toLocaleDateString() %></td>
                                <td><%= task.date.toLocaleTimeString() %></td>
                                <td><a href="/task-manager/<%= user._id %>/add-task/tasks-list/delete-task/<%= task._id %>" class="fa fa-trash" aria-hidden="false"></a></td>
                                <td><a href="/task-manager/<%= user._id %>/add-task/tasks-list/update-task/<%= task._id %>" class="fa fa-pencil-square-o" aria-hidden="false"></a></td>
                            </tr>
                            <% }) %>
                            <% } else { %>
                                <td colspan="6">No Tasks Found.</td>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>   
            <a href="/task-manager/<%= user._id %>" id="taskManagerLink">Back to Task Manager</a>         
        </main>
        <footer>
            <p>copyright &copy; <%= new Date().getFullYear() %> | Developer - Mohit Talreja | Email - talrejamohit15@gmail.com</p>
        </footer>
    </body>
</html>